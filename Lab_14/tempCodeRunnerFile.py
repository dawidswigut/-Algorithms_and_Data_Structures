    # # Visualize the results
    # def plot_hull(points, hull):
    #     plt.figure()
    #     x, y = zip(*points)
    #     plt.scatter(x, y)
    #     for i in range(len(hull)):
    #         plt.plot([hull[i][0], hull[(i + 1) % len(hull)][0]], [hull[i][1], hull[(i + 1) % len(hull)][1]], 'r-')
    #     plt.show()

    # plot_hull(points1, hull1)